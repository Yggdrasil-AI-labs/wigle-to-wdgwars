__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
.envrc

# Local secrets / state - never commit
*.key
wdgwars.key
cooldown.json
hwm.json

# Sample/scratch data
*.csv
!example/*.csv
*.json
!example/*.json

# Local host-specific venv wrappers (hardcode absolute local paths)
run-venv.sh
wdgwars-run

# RTK leftovers. The hook that wrote these was removed 2026-09-13; the
# db carries local paths and has no business in a public repo.
rtk/
