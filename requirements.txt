# Required: shared transport client for wdgwars.pl (HMAC envelope, retry,
# cooldown, silent-drop detection, key management). Same library Muninn
# v2.0 sits on.
#
# Pinned to v0.1.3 for reproducibility. Bumped deliberately when we
# upgrade gungnir. Switch to a normal `gungnir>=X.Y` pin once gungnir
# publishes to PyPI.
#
# Uses GitHub's archive tarball URL (not `git+https://`) so end users
# don't need git installed — pip fetches the .tar.gz over plain HTTPS.
gungnir @ https://github.com/Yggdrasil-AI-labs/gungnir/archive/refs/tags/v0.1.3.tar.gz
